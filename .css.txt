@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap');

:root {
  --bg-primary: #f0f2f5;
  --bg-secondary: #ffffff;
  --bg-card: rgba(255, 255, 255, 0.85);
  --bg-glass: rgba(255, 255, 255, 0.25);
  --text-primary: #1a1a2e;
  --text-secondary: #4a4a6a;
  --text-muted: #8a8aa0;
  --border-color: rgba(0, 0, 0, 0.08);
  --shadow: 0 8px 32px rgba(0, 0, 0, 0.06);
  --shadow-hover: 0 12px 48px rgba(0, 0, 0, 0.1);
  --accent: #6C63FF;
  --accent-hover: #5a52e0;
  --accent-light: rgba(108, 99, 255, 0.1);
  --success: #51CF66;
  --warning: #FCC419;
  --danger: #FF6B6B;
  --info: #22B8CF;
  --gradient-1: linear-gradient(135deg, #6C63FF, #F06595);
  --gradient-2: linear-gradient(135deg, #51CF66, #20C997);
  --gradient-3: linear-gradient(135deg, #FF922B, #FCC419);
  --gradient-4: linear-gradient(135deg, #22B8CF, #6C63FF);
  --nav-height: 70px;
  --radius: 16px;
  --radius-sm: 10px;
  --radius-xs: 6px;
  --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

[data-theme="dark"] {
  --bg-primary: #0a0a1a;
  --bg-secondary: #16162a;
  --bg-card: rgba(22, 22, 42, 0.85);
  --bg-glass: rgba(255, 255, 255, 0.05);
  --text-primary: #e8e8f0;
  --text-secondary: #a0a0c0;
  --text-muted: #606080;
  --border-color: rgba(255, 255, 255, 0.06);
  --shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
  --shadow-hover: 0 12px 48px rgba(0, 0, 0, 0.3);
  --accent-light: rgba(108, 99, 255, 0.15);
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  background: var(--bg-primary);
  color: var(--text-primary);
  transition: var(--transition);
  min-height: 100vh;
  overflow-x: hidden;
}

::selection { background: var(--accent); color: white; }

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: var(--text-muted); border-radius: 3px; }

a { color: var(--accent); text-decoration: none; transition: var(--transition); }
a:hover { color: var(--accent-hover); }

.glass { background: var(--bg-card); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 1px solid var(--border-color); box-shadow: var(--shadow); }
.glass-card { background: var(--bg-card); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 1px solid var(--border-color); border-radius: var(--radius); box-shadow: var(--shadow); transition: var(--transition); }
.glass-card:hover { transform: translateY(-2px); box-shadow: var(--shadow-hover); }

.gradient-text { background: var(--gradient-1); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }

.navbar-trendverse {
  height: var(--nav-height);
  background: var(--bg-secondary);
  border-bottom: 1px solid var(--border-color);
  padding: 0 24px;
  position: sticky;
  top: 0;
  z-index: 1000;
  transition: var(--transition);
}

.navbar-trendverse .navbar-brand { font-weight: 800; font-size: 1.4rem; background: var(--gradient-1); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; letter-spacing: -0.5px; }

.navbar-trendverse .nav-link { color: var(--text-secondary); font-weight: 500; padding: 8px 16px; border-radius: var(--radius-sm); transition: var(--transition); font-size: 0.9rem; }
.navbar-trendverse .nav-link:hover, .navbar-trendverse .nav-link.active { color: var(--accent); background: var(--accent-light); }
.navbar-trendverse .nav-link i { margin-right: 6px; }

.theme-toggle { width: 40px; height: 40px; border-radius: 50%; border: 1px solid var(--border-color); background: var(--bg-glass); color: var(--text-secondary); cursor: pointer; display: flex; align-items: center; justify-content: center; transition: var(--transition); font-size: 1.1rem; }
.theme-toggle:hover { background: var(--accent-light); color: var(--accent); }

.hero-section {
  padding: 60px 0 40px;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.hero-section h1 { font-size: 3rem; font-weight: 800; letter-spacing: -1px; margin-bottom: 12px; }
.hero-section p { font-size: 1.15rem; color: var(--text-secondary); max-width: 600px; margin: 0 auto 28px; line-height: 1.6; }

.hero-bg-shapes { position: absolute; inset: 0; overflow: hidden; pointer-events: none; z-index: -1; }
.hero-bg-shapes .shape { position: absolute; border-radius: 50%; opacity: 0.08; }
.hero-bg-shapes .shape:nth-child(1) { width: 400px; height: 400px; background: var(--accent); top: -100px; right: -100px; animation: float 8s ease-in-out infinite; }
.hero-bg-shapes .shape:nth-child(2) { width: 300px; height: 300px; background: var(--success); bottom: -50px; left: -50px; animation: float 10s ease-in-out infinite reverse; }
.hero-bg-shapes .shape:nth-child(3) { width: 200px; height: 200px; background: var(--warning); top: 50%; left: 10%; animation: float 12s ease-in-out infinite 2s; }

@keyframes float { 0%, 100% { transform: translateY(0) rotate(0deg); } 50% { transform: translateY(-30px) rotate(5deg); } }

.search-container { max-width: 700px; margin: 0 auto; position: relative; }
.search-container .input-group { background: var(--bg-card); backdrop-filter: blur(20px); border: 2px solid var(--border-color); border-radius: 60px; overflow: hidden; box-shadow: var(--shadow); transition: var(--transition); }
.search-container .input-group:focus-within { border-color: var(--accent); box-shadow: 0 0 0 4px var(--accent-light); }
.search-container .input-group-text { background: transparent; border: none; color: var(--text-muted); padding-left: 20px; }
.search-container .form-control { background: transparent; border: none; padding: 16px 8px; font-size: 1.05rem; color: var(--text-primary); outline: none; box-shadow: none; }
.search-container .form-control::placeholder { color: var(--text-muted); }
.search-container .btn-search { background: var(--gradient-1); border: none; color: white; padding: 12px 32px; border-radius: 60px; margin: 4px; font-weight: 600; cursor: pointer; transition: var(--transition); }
.search-container .btn-search:hover { transform: scale(1.02); box-shadow: 0 4px 16px rgba(108, 99, 255, 0.4); }

.search-suggestions { position: absolute; top: calc(100% + 8px); left: 0; right: 0; background: var(--bg-card); backdrop-filter: blur(20px); border: 1px solid var(--border-color); border-radius: var(--radius); box-shadow: var(--shadow-hover); max-height: 400px; overflow-y: auto; display: none; z-index: 100; }
.search-suggestions.show { display: block; }
.search-suggestions .suggestion-item { padding: 12px 20px; cursor: pointer; display: flex; align-items: center; gap: 12px; transition: var(--transition); color: var(--text-primary); }
.search-suggestions .suggestion-item:hover { background: var(--accent-light); color: var(--accent); }
.search-suggestions .suggestion-item i { color: var(--text-muted); width: 20px; }

.stats-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin: 40px 0; }
.stat-card { text-align: center; padding: 24px; }
.stat-card .stat-number { font-size: 2.2rem; font-weight: 800; background: var(--gradient-1); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
.stat-card .stat-label { font-size: 0.9rem; color: var(--text-muted); margin-top: 4px; text-transform: uppercase; letter-spacing: 0.5px; }
.stat-card .stat-icon { font-size: 2rem; color: var(--accent); margin-bottom: 12px; opacity: 0.5; }

.section-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; }
.section-header h2 { font-size: 1.5rem; font-weight: 700; }
.section-header .section-link { font-size: 0.9rem; color: var(--accent); font-weight: 500; cursor: pointer; }

.category-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); gap: 16px; margin-bottom: 40px; }
.category-card { padding: 20px 16px; text-align: center; cursor: pointer; border-radius: var(--radius); transition: var(--transition); }
.category-card:hover { transform: translateY(-4px) scale(1.02); }
.category-card .cat-icon { font-size: 1.8rem; margin-bottom: 8px; }
.category-card .cat-name { font-size: 0.85rem; font-weight: 600; }

.trend-card { padding: 20px; display: flex; align-items: center; gap: 16px; cursor: pointer; margin-bottom: 12px; }
.trend-card .trend-rank { font-size: 1.4rem; font-weight: 800; color: var(--text-muted); width: 40px; text-align: center; }
.trend-card .trend-info { flex: 1; }
.trend-card .trend-keyword { font-weight: 600; font-size: 1.05rem; }
.trend-card .trend-meta { font-size: 0.8rem; color: var(--text-muted); margin-top: 2px; }
.trend-card .trend-badge { font-size: 0.75rem; padding: 4px 10px; border-radius: 20px; font-weight: 500; }
.trend-card .trend-badge.up { background: rgba(81, 207, 102, 0.15); color: var(--success); }
.trend-card .trend-badge.down { background: rgba(255, 107, 107, 0.15); color: var(--danger); }
.trend-card .trend-badge.stable { background: rgba(252, 196, 25, 0.15); color: var(--warning); }

.platform-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 16px; }
.platform-card { padding: 20px; display: flex; flex-direction: column; gap: 12px; }
.platform-card .platform-header { display: flex; align-items: center; gap: 12px; }
.platform-card .platform-icon { width: 44px; height: 44px; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 1.3rem; color: white; flex-shrink: 0; }
.platform-card .platform-name { font-weight: 600; font-size: 1rem; }
.platform-card .platform-actions { display: flex; gap: 8px; flex-wrap: wrap; }
.platform-card .platform-actions .btn { font-size: 0.8rem; padding: 6px 14px; border-radius: var(--radius-xs); }

.sidebar-section { padding: 24px; border-radius: var(--radius); margin-bottom: 20px; }
.sidebar-section h5 { font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.5px; color: var(--text-muted); margin-bottom: 12px; }
.sidebar-section .sidebar-item { padding: 8px 12px; display: flex; align-items: center; gap: 10px; border-radius: var(--radius-xs); cursor: pointer; transition: var(--transition); color: var(--text-primary); font-size: 0.9rem; }
.sidebar-section .sidebar-item:hover { background: var(--accent-light); color: var(--accent); }
.sidebar-section .sidebar-item i { width: 20px; color: var(--text-muted); }

.chart-container { padding: 20px; border-radius: var(--radius); margin-bottom: 20px; }
.chart-container canvas { max-height: 300px; }

.content-generator-section { padding: 24px; border-radius: var(--radius); }
.content-generator-section .generated-item { padding: 16px; border-radius: var(--radius-sm); border: 1px solid var(--border-color); margin-bottom: 10px; cursor: pointer; transition: var(--transition); display: flex; justify-content: space-between; align-items: center; gap: 12px; }
.content-generator-section .generated-item:hover { border-color: var(--accent); background: var(--accent-light); }
.content-generator-section .generated-item .copy-btn { background: transparent; border: 1px solid var(--border-color); border-radius: var(--radius-xs); padding: 6px 12px; cursor: pointer; color: var(--text-muted); transition: var(--transition); font-size: 0.85rem; }
.content-generator-section .generated-item .copy-btn:hover { border-color: var(--accent); color: var(--accent); }

.hook-card { padding: 20px; border-radius: var(--radius-sm); border: 1px solid var(--border-color); transition: var(--transition); cursor: pointer; }
.hook-card:hover { border-color: var(--accent); transform: translateY(-2px); box-shadow: var(--shadow); }
.hook-card .hook-category { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.5px; color: var(--accent); font-weight: 600; }
.hook-card .hook-text { font-size: 1rem; margin-top: 8px; line-height: 1.5; font-weight: 500; }

.tool-card { padding: 24px; border-radius: var(--radius); text-align: center; transition: var(--transition); cursor: pointer; }
.tool-card:hover { transform: translateY(-4px); box-shadow: var(--shadow-hover); }
.tool-card .tool-icon { font-size: 2.2rem; margin-bottom: 12px; color: var(--accent); }
.tool-card .tool-name { font-weight: 600; font-size: 1rem; }
.tool-card .tool-desc { font-size: 0.85rem; color: var(--text-muted); margin-top: 4px; }

.form-card { padding: 24px; border-radius: var(--radius); }
.form-card .form-label { font-weight: 500; font-size: 0.9rem; color: var(--text-secondary); }
.form-card .form-control, .form-card .form-select { background: var(--bg-primary); border: 1px solid var(--border-color); border-radius: var(--radius-xs); padding: 10px 14px; color: var(--text-primary); transition: var(--transition); }
.form-card .form-control:focus, .form-card .form-select:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-light); background: var(--bg-secondary); }
.form-card textarea.form-control { min-height: 100px; resize: vertical; }

.btn-primary-custom { background: var(--gradient-1); border: none; color: white; padding: 10px 24px; border-radius: var(--radius-sm); font-weight: 600; cursor: pointer; transition: var(--transition); }
.btn-primary-custom:hover { transform: translateY(-1px); box-shadow: 0 4px 16px rgba(108, 99, 255, 0.4); }
.btn-outline-custom { background: transparent; border: 1px solid var(--border-color); color: var(--text-primary); padding: 10px 24px; border-radius: var(--radius-sm); font-weight: 500; cursor: pointer; transition: var(--transition); }
.btn-outline-custom:hover { border-color: var(--accent); color: var(--accent); background: var(--accent-light); }
.btn-danger-custom { background: var(--danger); border: none; color: white; padding: 6px 14px; border-radius: var(--radius-xs); cursor: pointer; transition: var(--transition); }
.btn-danger-custom:hover { opacity: 0.9; }
.btn-sm-custom { padding: 6px 14px; font-size: 0.85rem; border-radius: var(--radius-xs); }

.tabs-container { display: flex; gap: 4px; background: var(--bg-primary); padding: 4px; border-radius: var(--radius-sm); margin-bottom: 20px; flex-wrap: wrap; }
.tabs-container .tab-btn { padding: 8px 20px; border: none; background: transparent; color: var(--text-secondary); border-radius: var(--radius-xs); cursor: pointer; font-weight: 500; transition: var(--transition); font-size: 0.9rem; }
.tabs-container .tab-btn:hover { color: var(--accent); }
.tabs-container .tab-btn.active { background: var(--bg-card); color: var(--accent); box-shadow: 0 2px 8px rgba(0,0,0,0.06); }

.toast-container { position: fixed; bottom: 24px; right: 24px; z-index: 9999; }
.toast-msg { padding: 14px 24px; border-radius: var(--radius-sm); background: var(--bg-secondary); box-shadow: var(--shadow-hover); border: 1px solid var(--border-color); margin-top: 8px; font-size: 0.9rem; display: flex; align-items: center; gap: 10px; animation: slideInUp 0.3s ease; max-width: 360px; }
.toast-msg.success { border-left: 3px solid var(--success); }
.toast-msg.error { border-left: 3px solid var(--danger); }
.toast-msg.info { border-left: 3px solid var(--info); }

@keyframes slideInUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

.empty-state { text-align: center; padding: 60px 20px; color: var(--text-muted); }
.empty-state i { font-size: 3rem; margin-bottom: 16px; opacity: 0.5; }
.empty-state p { font-size: 1.05rem; }

.keyboard-hint { position: fixed; bottom: 24px; left: 24px; background: var(--bg-card); backdrop-filter: blur(20px); border: 1px solid var(--border-color); border-radius: var(--radius-sm); padding: 10px 16px; font-size: 0.8rem; color: var(--text-muted); box-shadow: var(--shadow); z-index: 100; display: flex; gap: 12px; align-items: center; }
.keyboard-hint kbd { background: var(--bg-primary); padding: 2px 8px; border-radius: 4px; font-family: 'JetBrains Mono', monospace; font-size: 0.75rem; border: 1px solid var(--border-color); }

.modal-trendverse .modal-content { background: var(--bg-secondary); border: 1px solid var(--border-color); border-radius: var(--radius); box-shadow: var(--shadow-hover); }
.modal-trendverse .modal-header { border-bottom: 1px solid var(--border-color); padding: 20px 24px; }
.modal-trendverse .modal-body { padding: 24px; }
.modal-trendverse .modal-footer { border-top: 1px solid var(--border-color); padding: 16px 24px; }
.modal-trendverse .btn-close { filter: invert(var(--close-btn-invert, 0)); }

.notes-area { min-height: 150px; background: var(--bg-primary); border: 1px solid var(--border-color); border-radius: var(--radius-xs); padding: 16px; color: var(--text-primary); resize: vertical; width: 100%; font-family: 'Inter', sans-serif; line-height: 1.6; transition: var(--transition); }
.notes-area:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-light); outline: none; }

.bookmark-item { padding: 14px 18px; border-radius: var(--radius-sm); border: 1px solid var(--border-color); margin-bottom: 8px; display: flex; justify-content: space-between; align-items: center; transition: var(--transition); }
.bookmark-item:hover { border-color: var(--accent); background: var(--accent-light); }
.bookmark-item .bookmark-title { font-weight: 500; }
.bookmark-item .bookmark-meta { font-size: 0.8rem; color: var(--text-muted); }
.bookmark-item .bookmark-actions { display: flex; gap: 8px; }

.calendar-grid { display: grid; grid-template-columns: repeat(7, 1fr); gap: 4px; }
.calendar-grid .cal-header { text-align: center; font-weight: 600; font-size: 0.8rem; padding: 8px 0; color: var(--text-muted); }
.calendar-grid .cal-day { aspect-ratio: 1; display: flex; align-items: center; justify-content: center; border-radius: var(--radius-xs); cursor: pointer; transition: var(--transition); font-size: 0.9rem; border: 1px solid transparent; }
.calendar-grid .cal-day:hover { background: var(--accent-light); border-color: var(--accent); }
.calendar-grid .cal-day.today { background: var(--accent); color: white; font-weight: 600; }
.calendar-grid .cal-day.other-month { color: var(--text-muted); opacity: 0.4; }
.calendar-grid .cal-day.has-event { border-color: var(--success); }

.affiliate-table { width: 100%; border-collapse: collapse; }
.affiliate-table th { text-align: left; padding: 12px 16px; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.5px; color: var(--text-muted); border-bottom: 1px solid var(--border-color); }
.affiliate-table td { padding: 12px 16px; border-bottom: 1px solid var(--border-color); font-size: 0.9rem; }

.footer-trendverse { padding: 40px 0; border-top: 1px solid var(--border-color); margin-top: 60px; text-align: center; color: var(--text-muted); font-size: 0.9rem; }

.tag { display: inline-block; padding: 4px 12px; border-radius: 20px; font-size: 0.8rem; font-weight: 500; margin: 2px; background: var(--accent-light); color: var(--accent); }

.progress-tracker { height: 8px; border-radius: 4px; background: var(--bg-primary); overflow: hidden; }
.progress-tracker .progress-fill { height: 100%; border-radius: 4px; background: var(--gradient-1); transition: width 0.6s ease; }

.opportunity-badge { display: inline-flex; align-items: center; gap: 4px; padding: 2px 10px; border-radius: 20px; font-size: 0.75rem; font-weight: 500; }
.opportunity-badge.high { background: rgba(81, 207, 102, 0.15); color: var(--success); }
.opportunity-badge.medium { background: rgba(252, 196, 25, 0.15); color: var(--warning); }
.opportunity-badge.low { background: rgba(255, 107, 107, 0.15); color: var(--danger); }

@media (max-width: 768px) {
  .hero-section h1 { font-size: 2rem; }
  .hero-section p { font-size: 1rem; }
  .stats-row { grid-template-columns: repeat(2, 1fr); gap: 12px; }
  .category-grid { grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); }
  .platform-grid { grid-template-columns: 1fr; }
  .navbar-trendverse .navbar-nav { background: var(--bg-secondary); padding: 12px; border-radius: var(--radius); }
  .search-container .btn-search span { display: none; }
  .keyboard-hint { display: none; }
  .container { padding-left: 16px; padding-right: 16px; }
}

@media (max-width: 576px) {
  .stats-row { grid-template-columns: 1fr 1fr; gap: 8px; }
  .category-grid { grid-template-columns: repeat(3, 1fr); gap: 8px; }
}

.fade-in { animation: fadeIn 0.5s ease; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

.pulse-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--success); display: inline-block; animation: pulse 2s infinite; }
@keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.4; } }
